{
  "name": "@agi-system/skills-registry",
  "version": "0.1.0",
  "description": "Skills Registry - registry, executor, validator, evolution, promotion pipeline candidate->sandbox->eval->approval->promote",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@agi-system/kernel": "*"
  }
}
