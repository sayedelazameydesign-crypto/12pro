{
  "name": "@agi-system/mission-ledger",
  "version": "0.1.0",
  "description": "Mission Ledger - goal, constraints, plan, steps, tool calls, decisions, artifacts, errors, approvals, tests, evidence, cost, duration, persistence",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {}
}
