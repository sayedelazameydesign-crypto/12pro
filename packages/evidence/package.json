{
  "name": "@agi-system/evidence",
  "version": "0.1.0",
  "description": "Evidence layer - journal, provenance, hashes, verification, certification gates, attestations, SBOM",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@agi-system/kernel": "*"
  }
}
